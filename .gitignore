# Runtime data — large, always changing, not tracked
/particle_data.csv
/particle_data_archive.csv
/particle_data_live.csv
/sync_log.txt

# Permanent archive — all particle measurements, local only (never pushed to GitHub)
data/measurement_archive.csv

# Sync state tracker — local only
data/counter_state.json

# Alert runtime state + logs (features/alerts) — per-host, never committed
data/alert_state.json
alert_log.txt
alert_cron.log

# Old dashboard subdirectory (now generated at repo root instead)
# Leading / anchors to repo root — does NOT match features/dashboard/
/dashboard/
misc/dashboard/

# Scratch output from test_atomic_write.py — regenerated on every run
.test_out/

# Python
__pycache__/
*.pyc
*.pyo
.env

# Secrets — credentials file, lives only on noether, never committed to GitHub
features/alerts/alerts_secrets.py

# Local configuration — user-specific settings, not tracked
config.local.yaml
features/temp_humidity_sensor/sensors.yaml

# Local working notes — the ONE ongoing-work file (all other md files besides
# README.md were removed 2026-07-07); keep local only, don't publish
ONGOING_WORK.md
# Session-by-session log of local dashboard changes — local only
tracking.md
CLEANUP_GITHUB.sh

# Noether-only full-history dashboard output (local_serve.py) — NEVER push:
# it contains the complete archive, not the public 30-day window
index_local.html

# macOS
.DS_Store
